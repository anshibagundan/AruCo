CREATE TABLE actions (
                         id SERIAL PRIMARY KEY,
                         difficulty INTEGER,
                         lef_sel VARCHAR(255),
                         rig_sel VARCHAR(255)
);