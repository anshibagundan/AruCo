CREATE TABLE quizzes (
                         id SERIAL PRIMARY KEY,
                         name VARCHAR(255),
                         difficulty INTEGER,
                         lef_sel VARCHAR(255),
                         rig_sel VARCHAR(255)
);